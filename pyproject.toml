[tool.ruff]
line-length = 120

[tool.ruff.lint.per-file-ignores]
# freeze_support() and the sys.path.insert() for scrapers.* must run before
# the imports that depend on them - the import order is intentional here.
"backend/main.py" = ["E402"]
